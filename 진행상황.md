# 세문 경정청구 랜딩페이지 — 진행 상황 (2026-09-24 기준)

## ✅ 완료된 것
| 항목 | 내용 |
|---|---|
| 신청서 → Gmail/시트 연동 | Apps Script 코드: `apps-script/Code.gs` (알림 메일: choegyujeong783@gmail.com, 시트: 구글 드라이브 「랜딩 신청서」) |
| Apps Script 웹앱 주소 | `https://script.google.com/macros/s/AKfycbyQcf_3af5HNcN4CXET5uhcoPNcyppG-kqzNiSj-oHY2U6xihWK2I7faOcwgskJiUaa/exec` |
| 랜딩페이지 파일 | `경정청구.html` (위 주소 적용됨) → 업로드할 때는 **`index.html`** 로 이름 바꿔 올림 |
| 카페24 도메인 | `semoon.store` (네임서버: 카페24, 만료 2027-09-16) |
| 카페24 웹호스팅 | 아이디 `kbss15162606`, 기본주소 `kbss15162606.mycafe24.com` (연결 상태 정상) |

## ⛔ 막힌 곳
호스팅 「도메인 연결하기」에서 `semoon.store` 입력 시
**"인증 대기중인 도메인은 연결할 수 없습니다"** → 도메인 **소유자 이메일 인증**이 아직 안 됨.

## 📋 내일 할 일 (순서대로)
1. **도메인 소유자 인증**
   - 도메인 소유자 이메일 받은편지함·스팸함에서 인증 메일(“도메인 소유자 인증”, “Verify your domain” 등) 찾아 링크 클릭
   - 없으면: 카페24 나의서비스관리 → 도메인 관리(도메인 구입 아이디) → semoon.store 선택 → 소유자 정보변경 / 인증메일 재발송
   - ⚠️ 보통 15일 안에 인증 안 하면 도메인 정지될 수 있음
2. **호스팅에 도메인 연결**
   - 나의서비스관리 → 호스팅 아이디(`kbss15162606`) → 도메인 연결 관리 → `semoon.store` 입력 → 연결하기
   - `www.semoon.store` 도 추가
   - 연결 상태가 「경고/오류」면 **[진단 결과]** 캡처해서 Claude에게 보여주기
3. **DNS 정리 (필요 시)**
   - 도메인 DNS 관리 → A레코드에 `185.199.x.x`(GitHub용) 넣었다면 삭제/수정 → 진단 결과에 나오는 **호스팅 서버 IP** 로
   - CNAME `www → choegyujeong783-tech.github.io` 넣었다면 삭제
4. **파일 업로드** (도메인 인증 전에도 가능)
   - 웹FTP 또는 파일질라: 호스트 `kbss15162606.mycafe24.com` / 사용자 `kbss15162606` / 포트 21 / FTP 비밀번호
   - `www` 폴더에 `index.html` 업로드 (기존 index.html 있으면 먼저 백업)
   - 확인: https://kbss15162606.mycafe24.com
5. **SSL(https)** — 도메인 연결 정상 후 호스팅 화면에서 인증서 발급/재발급 신청
6. **최종 테스트** — https://semoon.store 에서 신청서 제출 → Gmail 알림 + 시트 저장 확인
7. (선택) 연결 정상 후 `semoon.store` 를 **대표도메인으로 변경**

## 참고
- Apps Script 코드를 수정하면: 배포 → 배포 관리 → 연필 → 버전 「새 버전」 → 배포 (주소 유지)
- 에디터에서 테스트는 `testSetup` 함수로 (doPost 직접 실행하면 postData 오류 나는 게 정상)
- 파일질라는 광고 없는 버전 사용: https://filezilla-project.org/download.php?show_all=1
